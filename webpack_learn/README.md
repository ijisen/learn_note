# webpack 简介
__一款模块加载器兼打包工具__

## 一、安 装
```
全局安装
npm install webpack -g

依赖写入 package.json 包去更人性化：
npm init
npm install webpack --save-dev
```
## 二、配置
每个项目下都必须配置有一个 __webpack.config.js__ ，告诉 webpack 它需要做什么。

